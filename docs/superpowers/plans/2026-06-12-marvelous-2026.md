# Marvelous 2026 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construire un site statique Astro déployé sur GitHub Pages qui affiche le classement RTS du groupe « Marvelous » (CDM 2026), des stats/data-viz, et les pronostics de l'IA — alimenté par un scraper GitHub Actions du HTML public RTS.

**Architecture:** Pipeline data (scraper Node → snapshots JSON immuables → `latest.json` + `timeseries.json`) committé par un cron GitHub Actions, qui déclenche un build Astro statique déployé sur Pages. Toute la logique pure (parsing, agrégation, scoring, géométrie SVG) est en modules `.mjs` testés avec `node --test`, importés à la fois par les scripts et par les composants Astro.

**Tech Stack:** Astro (sortie statique), JavaScript/TypeScript, modules ESM `.mjs`, `node:test` (zéro dépendance de test), GitHub Actions, GitHub Pages. Aucune dépendance runtime de scraping (fetch natif Node 20 + regex). SVG fait main pour les graphes.

**Prérequis machine :** Node ≥ 20 (`node -v`). Compte GitHub confirmé.

**Spec de référence :** `docs/superpowers/specs/2026-06-12-marvelous-2026-design.md`

---

## Structure des fichiers (cible)

```
astro.config.mjs            # site/base GitHub Pages
package.json
tsconfig.json
.github/workflows/
  deploy.yml                # build + déploiement Pages (push + workflow_call)
  scrape.yml                # cron → scrape → commit data/ → appelle deploy
data/
  snapshots/                # <ISO>.json immuables
  latest.json               # état courant + deltas
  timeseries.json           # séries par membre pour les graphes
scripts/
  scrape.mjs                # entrée CLI : fetch + parse + écrit data/
  lib/
    parse.mjs               # parseCommunity(html) → objet structuré  (testé)
    aggregate.mjs           # buildLatest/buildTimeseries             (testé)
    scoring.mjs             # barème RTS, scorePrediction, ghost      (testé)
    svg.mjs                 # helpers géométrie (sparkline/ligne)      (testé)
    stats.mjs               # stats dérivées (grimpeur, momentum…)     (testé)
    members.mjs             # mapping id→nom connu + couleurs
test/
  fixtures/community.html
  parse.test.mjs
  aggregate.test.mjs
  scoring.test.mjs
  svg.test.mjs
  stats.test.mjs
src/
  config.ts                 # constantes site (URLs RTS, communauté)
  data.ts                   # chargement des JSON data/ au build
  styles/tokens.css         # design tokens Tri-Streak Noir
  layouts/Base.astro
  components/
    Header.astro Footer.astro Streaks.astro Trophy.astro
    Podium.astro LeaderboardRow.astro Sparkline.astro MovementArrow.astro
    RaceChart.astro BumpChart.astro HeadToHead.astro Badges.astro OtherGroups.astro
    MatchCard.astro Bracket.astro
  content/
    config.ts               # content collection "pronos"
    pronos/groupe-j1.md ...  # pronos IA par journée
  pages/
    index.astro             # Classement
    le-mag.astro            # Stats
    pronos-ia.astro         # Pronos de l'IA (L'Oracle)
    matchs.astro            # Calendrier
```

---

# Phase 0 — Scaffolding & déploiement vivant

Objectif : un site Astro minimal en ligne sur GitHub Pages dès la fin de la phase.

### Task 0.1 : Initialiser le projet

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `.gitignore` (déjà présent — vérifier)

- [ ] **Step 1 : Initialiser Astro (template vide, sans installer les exemples)**

Run:
```bash
pnpm create astro@latest . --template minimal --no-install --no-git --skip-houston --yes
pnpm install
```
Si le CLI refuse le dossier non vide, scaffolder manuellement (`package.json` avec scripts `dev`/`build`/`preview` + `"test": "node --test"`, `tsconfig.json` étendant `astro/tsconfigs/strict`, `src/pages/index.astro`) puis `pnpm install astro`. **Gestionnaire de paquets : pnpm uniquement** (jamais npm). `package.json` épinglé via `"packageManager": "pnpm@…"`. Approuver les builds natifs une fois : `pnpm approve-builds esbuild sharp` (écrit `allowBuilds` dans `pnpm-workspace.yaml`, indispensable pour que `pnpm install` réussisse en CI).

- [ ] **Step 2 : Configurer `astro.config.mjs` pour GitHub Pages**

Remplacer le contenu par (⚠️ remplacer `MON-USER` et `MON-REPO` par le vrai compte/nom de repo GitHub) :
```js
import { defineConfig } from 'astro/config';

const GH_USER = 'nicolasloi';
const GH_REPO = 'CDM-prono';

export default defineConfig({
  site: `https://${GH_USER}.github.io`,
  base: `/${GH_REPO}`,
  trailingSlash: 'ignore',
});
```

- [ ] **Step 3 : Vérifier le build**

Run: `pnpm run build`
Expected : `dist/` généré sans erreur.

- [ ] **Step 4 : Commit**

```bash
git add -A
git commit -m "chore: scaffold Astro project"
```

### Task 0.2 : Workflow de déploiement GitHub Pages

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1 : Écrire `deploy.yml` (déclenché sur push ET réutilisable via workflow_call)**

```yaml
name: Deploy

on:
  push:
    branches: [main]
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
      - id: deploy
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2 : Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: GitHub Pages deploy workflow"
```

- [ ] **Step 3 : (Manuel utilisateur) Activer Pages**

Après le premier push : GitHub → Settings → Pages → Source = **GitHub Actions**. Noter l'URL `https://MON-USER.github.io/MON-REPO/`.

### Task 0.3 : Design tokens + layout de base

**Files:**
- Create: `src/styles/tokens.css`, `src/config.ts`, `src/layouts/Base.astro`

- [ ] **Step 1 : Écrire `src/config.ts`**

```ts
export const SITE = {
  title: 'CDM Prono',
  company: 'Marvelous Digital',
  tagline: 'Le classement Coupe du Monde 2026 de Marvelous Digital',
  communityId: 484, // usage interne (scraping) uniquement — NE JAMAIS afficher le n° de groupe
  rtsCommunityUrl: 'https://pronostics.rts.ch/communities/484',
  rtsBaseUrl: 'https://pronostics.rts.ch',
};
```

- [ ] **Step 2 : Écrire `src/styles/tokens.css` (identité Tri-Streak Noir)**

```css
:root {
  --bg: #0a0e1a;
  --bg-soft: #11162a;
  --surface: rgba(255,255,255,.04);
  --surface-hi: rgba(255,255,255,.07);
  --line: #1a2138;
  --gold: #e8c66a;
  --gold-soft: rgba(232,198,106,.16);
  --text: #eef1f8;
  --text-dim: #8b93ab;
  --red: #e3342f;     /* Canada  */
  --green: #00a85a;   /* Mexique */
  --blue: #2b6fff;    /* USA     */
  --radius: 40px;
  --radius-card: 22px;
  --font: 'Helvetica Neue', -apple-system, system-ui, Arial, sans-serif;
}
* { box-sizing: border-box; }
html, body { margin: 0; }
body {
  background: var(--bg); color: var(--text); font-family: var(--font);
  -webkit-font-smoothing: antialiased; line-height: 1.4;
}
a { color: inherit; }
.container { width: min(720px, 92vw); margin: 0 auto; }
.kicker { font: 800 10px/1 var(--font); letter-spacing: .26em; color: var(--gold); text-transform: uppercase; }
.h1 { font-size: clamp(28px, 7vw, 40px); font-weight: 900; letter-spacing: -.02em; line-height: .92; margin: 8px 0; }
.h1 em { color: var(--gold); font-style: italic; }
.dim { color: var(--text-dim); }
.pill { background: var(--surface); border-radius: var(--radius); }
@media (prefers-reduced-motion: no-preference) {
  .streak-anim { animation: drift 9s ease-in-out infinite alternate; }
}
@keyframes drift { from { transform: translateX(-2%); } to { transform: translateX(2%); } }
```

- [ ] **Step 3 : Écrire `src/components/Streaks.astro` (rubans tricolores SVG réutilisables)**

```astro
---
// Bandeau de rubans tricolores animés. `height` en px.
const { height = 120 } = Astro.props;
---
<svg class="streak-anim" viewBox="0 0 400 120" preserveAspectRatio="none"
     style={`position:absolute;inset:0;width:100%;height:${height}px;opacity:.9;z-index:0;pointer-events:none`}>
  <path d="M-20 95 C90 60 150 120 420 70" stroke="var(--red)" stroke-width="7" fill="none" stroke-linecap="round"/>
  <path d="M-20 108 C120 80 160 130 420 88" stroke="var(--green)" stroke-width="7" fill="none" stroke-linecap="round"/>
  <path d="M-20 120 C100 100 200 140 420 104" stroke="var(--blue)" stroke-width="7" fill="none" stroke-linecap="round"/>
</svg>
```

- [ ] **Step 4 : Écrire `src/components/Header.astro` et `Footer.astro` (avec lien RTS)**

`Header.astro` :
```astro
---
import { SITE } from '../config';
const base = import.meta.env.BASE_URL;
const path = (p) => `${base}${p}`.replace(/\/{2,}/g, '/');
const nav = [
  { href: path('/'), label: 'Classement' },
  { href: path('/le-mag'), label: 'Le Mag' },
  { href: path('/pronos-ia'), label: 'Pronos IA' },
  { href: path('/matchs'), label: 'Matchs' },
];
---
<header style="position:relative;overflow:hidden;border-bottom:1px solid var(--line)">
  <div class="container" style="position:relative;z-index:1;padding:14px 0;display:flex;gap:16px;align-items:center;flex-wrap:wrap">
    <a href={nav[0].href} style="text-decoration:none;font-weight:900;letter-spacing:-.02em">
      CDM <span style="color:var(--gold)">Prono</span>
    </a>
    <nav style="display:flex;gap:14px;flex-wrap:wrap;font-size:13px;font-weight:600">
      {nav.map((n) => <a href={n.href} style="text-decoration:none;color:var(--text-dim)">{n.label}</a>)}
    </nav>
    <a href={SITE.rtsCommunityUrl} target="_blank" rel="noopener"
       style="margin-left:auto;font-size:12px;font-weight:700;color:var(--gold);text-decoration:none;border:1px solid var(--gold-soft);padding:6px 10px;border-radius:30px">
      Tableau officiel RTS ↗
    </a>
  </div>
</header>
```

`Footer.astro` :
```astro
---
import { SITE } from '../config';
---
<footer class="container dim" style="padding:28px 0;font-size:12px;border-top:1px solid var(--line);margin-top:40px">
  <p>Données : <a href={SITE.rtsCommunityUrl} target="_blank" rel="noopener" style="color:var(--gold)">jeu de pronostics RTS — communauté Marvelous ↗</a>. Site non officiel, fait par et pour l'équipe.</p>
</footer>
```

- [ ] **Step 5 : Écrire `src/layouts/Base.astro`**

```astro
---
import '../styles/tokens.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE } from '../config';
const { title } = Astro.props;
---
<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title ? `${title} · ${SITE.title}` : SITE.title}</title>
    <meta name="description" content={SITE.tagline} />
  </head>
  <body>
    <Header />
    <main class="container" style="padding:24px 0 0"><slot /></main>
    <Footer />
  </body>
</html>
```

- [ ] **Step 6 : Remplacer `src/pages/index.astro` par un placeholder utilisant le layout**

```astro
---
import Base from '../layouts/Base.astro';
---
<Base title="Classement">
  <p class="kicker">★ Marvelous Digital</p>
  <h1 class="h1">Le <em>Classement</em></h1>
  <p class="dim">Site en construction — bientôt le classement live.</p>
</Base>
```

- [ ] **Step 7 : Build + commit**

Run: `pnpm run build`
Expected : succès.
```bash
git add -A
git commit -m "feat: base layout + Tri-Streak Noir design tokens"
```

> **Jalon Phase 0 :** après push, le site placeholder est en ligne sur Pages. ✅

---

# Phase 1 — Pipeline de données

Toute la logique pure est en `.mjs` testée avec `node --test`.

### Task 1.1 : Outils de test + mapping membres

**Files:**
- Modify: `package.json` (script `test`)
- Create: `scripts/lib/members.mjs`

- [ ] **Step 1 : Ajouter le script de test dans `package.json`**

Dans `"scripts"`, ajouter : `"test": "node --test"`.

- [ ] **Step 2 : Écrire `scripts/lib/members.mjs` (couleur d'accent par rang, helpers)**

```js
// Couleurs tricolores cyclées pour les pastilles de rang.
export const ACCENTS = ['#e3342f', '#00a85a', '#2b6fff'];
export const accentFor = (i) => ACCENTS[i % ACCENTS.length];

// Normalise un nom pour servir de clé stable si l'id manque.
export const nameKey = (s) => s.normalize('NFKD').replace(/[^\w]/g, '').toLowerCase();
```

- [ ] **Step 3 : Commit**

```bash
git add package.json scripts/lib/members.mjs
git commit -m "chore: test runner + members helpers"
```

### Task 1.2 : Parser le HTML communauté (TDD)

**Files:**
- Create: `test/fixtures/community.html`, `test/parse.test.mjs`, `scripts/lib/parse.mjs`

- [ ] **Step 1 : Écrire la fixture `test/fixtures/community.html`** (reproduit la structure réelle observée : `#rang`, `Points totaux : N Pts`, nom, `admin?`, `N Pkt`, sections groupes & experts, liens `/users/<id>`)

```html
<html><body>
<section class="rankingList">
  <h2>Classement des membres</h2>
  <a class="rankingItem" href="/users/9Nz1"><span>#1</span><span>Points totaux : 18 Pts</span><span>Vitor Pinto</span><span>18 Pkt</span></a>
  <a class="rankingItem" href="/users/AbC2"><span>#2</span><span>Points totaux : 14 Pts</span><span>Joao M</span><span>14 Pkt</span></a>
  <a class="rankingItem" href="/users/AdM3"><span>#3</span><span>Points totaux : 11 Pts</span><span>Mehdi M</span><span>admin</span><span>11 Pkt</span></a>
  <a class="rankingItem" href="/users/Nic4"><span>#3</span><span>Points totaux : 11 Pts</span><span>Nicolas L</span><span>11 Pkt</span></a>
</section>
<section class="rankingList">
  <h2>Classement des groupes</h2>
  <div class="rankingItem"><span>#1</span><span>Sebulba Lalas</span><span>19 Pkt</span></div>
  <div class="rankingItem"><span>#5</span><span>Batraciens</span><span>18 Pkt</span></div>
</section>
<aside class="expertsSidebox"><span>Experts</span><span>12 Pts</span><span>16 Pts</span><span>18 Pts</span></aside>
</body></html>
```

- [ ] **Step 2 : Écrire `test/parse.test.mjs`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { parseCommunity } from '../scripts/lib/parse.mjs';

const html = readFileSync(new URL('./fixtures/community.html', import.meta.url), 'utf8');
const r = parseCommunity(html);

test('parse les membres avec rang, points, id', () => {
  assert.equal(r.members.length, 4);
  assert.deepEqual(r.members[0], { id: '9Nz1', name: 'Vitor Pinto', rank: 1, totalPoints: 18, isAdmin: false });
  assert.equal(r.members[2].name, 'Mehdi M');
  assert.equal(r.members[2].isAdmin, true);
  assert.equal(r.members[2].totalPoints, 11);
});

test('gère les rangs ex æquo', () => {
  assert.equal(r.members[2].rank, 3);
  assert.equal(r.members[3].rank, 3);
});

test('parse les autres groupes et les experts', () => {
  assert.deepEqual(r.otherGroups[0], { rank: 1, name: 'Sebulba Lalas', points: 19 });
  assert.deepEqual(r.experts, [12, 16, 18]);
});
```

- [ ] **Step 3 : Lancer le test (échec attendu)**

Run: `node --test test/parse.test.mjs`
Expected : FAIL (`parseCommunity` introuvable).

- [ ] **Step 4 : Écrire `scripts/lib/parse.mjs`**

```js
// Parsing défensif basé sur des ancres texte stables (insensible aux classes hashées RTS).

function stripToText(html) {
  return html
    .replace(/<script[\s\S]*?<\/script>/gi, ' ')
    .replace(/<style[\s\S]*?<\/style>/gi, ' ')
    .replace(/<[^>]+>/g, '\n')
    .replace(/&amp;/g, '&').replace(/&#39;/g, "'").replace(/&nbsp;/g, ' ')
    .split('\n').map((s) => s.trim()).filter(Boolean);
}

function sectionBounds(lines, startLabel, endLabels) {
  const start = lines.findIndex((l) => l.includes(startLabel));
  if (start === -1) return null;
  let end = lines.length;
  for (const lbl of endLabels) {
    const e = lines.findIndex((l, i) => i > start && l.includes(lbl));
    if (e !== -1 && e < end) end = e;
  }
  return { start, end };
}

export function parseCommunity(html) {
  const lines = stripToText(html);

  // --- Membres : motif #rang / "Points totaux : N Pts" / nom / [admin] / "N Pkt" ---
  const ids = (html.match(/\/users\/([A-Za-z0-9]+)/g) || []).map((s) => s.split('/').pop());
  const members = [];
  const mb = sectionBounds(lines, 'Classement des membres', ['Classement des groupes', 'Experts']);
  const range = mb ? lines.slice(mb.start, mb.end) : lines;
  for (let i = 0; i < range.length; i++) {
    const rm = /^#(\d+)$/.exec(range[i]);
    const pm = /^Points totaux\s*:\s*(\d+)/.exec(range[i + 1] || '');
    if (rm && pm) {
      const name = range[i + 2];
      const isAdmin = range[i + 3] === 'admin';
      members.push({
        id: ids[members.length] || null,
        name,
        rank: Number(rm[1]),
        totalPoints: Number(pm[1]),
        isAdmin,
      });
    }
  }

  // --- Autres groupes : #rang / nom / "N Pkt" ---
  const otherGroups = [];
  const gb = sectionBounds(lines, 'Classement des groupes', ['Experts', 'Afficher']);
  if (gb) {
    const g = lines.slice(gb.start, gb.end);
    for (let i = 0; i < g.length; i++) {
      const rm = /^#(\d+)$/.exec(g[i]);
      const km = /^(\d+)\s*Pkt$/.exec(g[i + 2] || '');
      if (rm && km && g[i + 1] && !/Pkt|Pts/.test(g[i + 1])) {
        otherGroups.push({ rank: Number(rm[1]), name: g[i + 1], points: Number(km[1]) });
      }
    }
  }

  // --- Experts : 3 valeurs "N Pts" après "Experts" ---
  const experts = [];
  const ei = lines.findIndex((l) => l === 'Experts');
  if (ei !== -1) {
    for (let i = ei + 1; i < lines.length && experts.length < 3; i++) {
      const m = /^(\d+)\s*Pts$/.exec(lines[i]);
      if (m) experts.push(Number(m[1]));
    }
  }

  return { members, otherGroups, experts };
}
```

- [ ] **Step 5 : Relancer le test (succès attendu)**

Run: `node --test test/parse.test.mjs`
Expected : PASS (3 tests).

- [ ] **Step 6 : Commit**

```bash
git add scripts/lib/parse.mjs test/parse.test.mjs test/fixtures/community.html
git commit -m "feat: parseCommunity defensive HTML parser (TDD)"
```

### Task 1.3 : Agrégation latest + timeseries (TDD)

**Files:**
- Create: `test/aggregate.test.mjs`, `scripts/lib/aggregate.mjs`

- [ ] **Step 1 : Écrire `test/aggregate.test.mjs`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildLatest, buildTimeseries } from '../scripts/lib/aggregate.mjs';

const snaps = [
  { takenAt: '2026-06-11T20:00:00Z', members: [
    { id: 'a', name: 'A', rank: 1, totalPoints: 10, isAdmin: false },
    { id: 'b', name: 'B', rank: 2, totalPoints: 8, isAdmin: false },
  ], otherGroups: [], experts: [12,16,18] },
  { takenAt: '2026-06-12T20:00:00Z', members: [
    { id: 'b', name: 'B', rank: 1, totalPoints: 14, isAdmin: false },
    { id: 'a', name: 'A', rank: 2, totalPoints: 11, isAdmin: false },
  ], otherGroups: [], experts: [12,16,18] },
];

test('buildLatest ajoute deltas points & rang depuis le snapshot précédent', () => {
  const latest = buildLatest(snaps);
  const b = latest.members.find((m) => m.id === 'b');
  assert.equal(b.rank, 1);
  assert.equal(b.pointsDelta, 6);   // 14 - 8
  assert.equal(b.rankDelta, 1);     // 2 -> 1 (gagne 1 place)
  const a = latest.members.find((m) => m.id === 'a');
  assert.equal(a.rankDelta, -1);
});

test('buildLatest sans précédent → deltas nuls', () => {
  const latest = buildLatest([snaps[0]]);
  assert.equal(latest.members[0].pointsDelta, 0);
  assert.equal(latest.members[0].rankDelta, 0);
});

test('buildTimeseries regroupe par membre dans le temps', () => {
  const ts = buildTimeseries(snaps);
  assert.deepEqual(ts.a.map((p) => p.totalPoints), [10, 11]);
  assert.deepEqual(ts.b.map((p) => p.rank), [2, 1]);
  assert.equal(ts.a[0].name, 'A');
});
```

- [ ] **Step 2 : Lancer (échec attendu)**

Run: `node --test test/aggregate.test.mjs`
Expected : FAIL.

- [ ] **Step 3 : Écrire `scripts/lib/aggregate.mjs`**

```js
import { nameKey } from './members.mjs';

const keyOf = (m) => m.id || nameKey(m.name);

export function buildLatest(snapshots) {
  const sorted = [...snapshots].sort((a, b) => a.takenAt.localeCompare(b.takenAt));
  const curr = sorted[sorted.length - 1];
  const prev = sorted[sorted.length - 2];
  const prevByKey = new Map((prev?.members || []).map((m) => [keyOf(m), m]));
  const members = curr.members.map((m) => {
    const p = prevByKey.get(keyOf(m));
    return {
      ...m,
      pointsDelta: p ? m.totalPoints - p.totalPoints : 0,
      rankDelta: p ? p.rank - m.rank : 0, // positif = monte
    };
  });
  return { takenAt: curr.takenAt, members, otherGroups: curr.otherGroups, experts: curr.experts };
}

export function buildTimeseries(snapshots) {
  const sorted = [...snapshots].sort((a, b) => a.takenAt.localeCompare(b.takenAt));
  const series = {};
  for (const snap of sorted) {
    for (const m of snap.members) {
      const k = keyOf(m);
      (series[k] ||= []).push({ takenAt: snap.takenAt, totalPoints: m.totalPoints, rank: m.rank, name: m.name });
    }
  }
  return series;
}
```

- [ ] **Step 4 : Relancer (succès attendu)**

Run: `node --test test/aggregate.test.mjs`
Expected : PASS.

- [ ] **Step 5 : Commit**

```bash
git add scripts/lib/aggregate.mjs test/aggregate.test.mjs
git commit -m "feat: aggregate latest + timeseries (TDD)"
```

### Task 1.4 : Script de scrape CLI

**Files:**
- Create: `scripts/scrape.mjs`
- Create: `data/snapshots/.gitkeep`

- [ ] **Step 1 : Écrire `scripts/scrape.mjs`**

```js
import { mkdirSync, writeFileSync, readFileSync, readdirSync, existsSync } from 'node:fs';
import { parseCommunity } from './lib/parse.mjs';
import { buildLatest, buildTimeseries } from './lib/aggregate.mjs';

const COMMUNITY_URL = 'https://pronostics.rts.ch/communities/484';
const DATA = new URL('../data/', import.meta.url);
const SNAPS = new URL('../data/snapshots/', import.meta.url);

async function fetchHtml(url) {
  const res = await fetch(url, { headers: { 'User-Agent': 'Mozilla/5.0 MarvelousBot' } });
  if (!res.ok) throw new Error(`HTTP ${res.status} for ${url}`);
  return res.text();
}

function readSnapshots() {
  if (!existsSync(SNAPS)) return [];
  return readdirSync(SNAPS)
    .filter((f) => f.endsWith('.json'))
    .map((f) => JSON.parse(readFileSync(new URL(f, SNAPS), 'utf8')));
}

async function main() {
  mkdirSync(SNAPS, { recursive: true });
  const html = await fetchHtml(COMMUNITY_URL);
  const parsed = parseCommunity(html);

  if (!parsed.members.length) {
    console.error('Aucun membre parsé — HTML probablement changé. Abandon sans écrire.');
    process.exit(1); // ne corrompt pas latest.json
  }

  const takenAt = new Date().toISOString();
  const snapshot = { takenAt, ...parsed };
  const fname = `${takenAt.replace(/[:.]/g, '-')}.json`;
  writeFileSync(new URL(fname, SNAPS), JSON.stringify(snapshot, null, 2));

  const all = readSnapshots();
  writeFileSync(new URL('latest.json', DATA), JSON.stringify(buildLatest(all), null, 2));
  writeFileSync(new URL('timeseries.json', DATA), JSON.stringify(buildTimeseries(all), null, 2));

  console.log(`OK : ${parsed.members.length} membres, snapshot ${fname}`);
}

main().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 2 : Créer le dossier snapshots versionné**

Run: `mkdir -p data/snapshots && touch data/snapshots/.gitkeep`

- [ ] **Step 3 : Test de fumée réel (réseau)**

Run: `node scripts/scrape.mjs`
Expected : `OK : N membres, snapshot …json`, et `data/latest.json` + `data/timeseries.json` créés avec les vrais membres Marvelous. Vérifier visuellement `data/latest.json`.

- [ ] **Step 4 : Commit (code + premier vrai snapshot)**

```bash
git add scripts/scrape.mjs data/
git commit -m "feat: scrape.mjs CLI + premier snapshot live"
```

### Task 1.5 : Workflow cron de scrape

**Files:**
- Create: `.github/workflows/scrape.yml`

- [ ] **Step 1 : Écrire `scrape.yml` (cron → scrape → commit data/ → appelle deploy)**

```yaml
name: Scrape RTS

on:
  schedule:
    - cron: '0 16-23 * * *'   # 18h→00h CEST, horaire
    - cron: '0 0-4 * * *'     # 00h→06h CEST, horaire
    - cron: '0 7,10,13 * * *' # journée, toutes les 3h
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.commit.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: node scripts/scrape.mjs
      - id: commit
        run: |
          git config user.name "marvelous-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "data: snapshot $(date -u +%FT%TZ)"
            git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

  deploy:
    needs: scrape
    if: needs.scrape.outputs.changed == 'true'
    uses: ./.github/workflows/deploy.yml
    permissions:
      contents: read
      pages: write
      id-token: write
```

- [ ] **Step 2 : Commit**

```bash
git add .github/workflows/scrape.yml
git commit -m "ci: cron scrape workflow + deploy on data change"
```

- [ ] **Step 3 : (Manuel) Vérifier après push**

GitHub → Actions → lancer `Scrape RTS` manuellement (`workflow_dispatch`). Vérifier que data/ se met à jour et que le déploiement suit.

> **Jalon Phase 1 :** la donnée se met à jour automatiquement. ✅

---

# Phase 2 — Page Classement

### Task 2.1 : Chargement des données au build

**Files:**
- Create: `src/data.ts`

- [ ] **Step 1 : Écrire `src/data.ts`**

```ts
import latest from '../data/latest.json';
import timeseries from '../data/timeseries.json';
import { nameKey } from '../scripts/lib/members.mjs';

export type Member = {
  id: string | null; name: string; rank: number; totalPoints: number;
  isAdmin: boolean; pointsDelta: number; rankDelta: number;
};
export type Latest = {
  takenAt: string; members: Member[];
  otherGroups: { rank: number; name: string; points: number }[];
  experts: number[];
};
export type TsPoint = { takenAt: string; totalPoints: number; rank: number; name: string };

export const getLatest = (): Latest => latest as Latest;
export const getTimeseries = (): Record<string, TsPoint[]> => timeseries as any;
export const keyOf = (m: { id: string | null; name: string }) => m.id || nameKey(m.name);
```

- [ ] **Step 2 : Vérifier le build**

Run: `pnpm run build`
Expected : succès (les JSON sont importés).

- [ ] **Step 3 : Commit**

```bash
git add src/data.ts
git commit -m "feat: build-time data loaders"
```

### Task 2.2 : Helpers géométrie SVG (TDD)

**Files:**
- Create: `test/svg.test.mjs`, `scripts/lib/svg.mjs`

- [ ] **Step 1 : Écrire `test/svg.test.mjs`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { linePath, scaleY } from '../scripts/lib/svg.mjs';

test('scaleY mappe min→bas et max→haut (y inversé)', () => {
  assert.equal(scaleY(0, 0, 10, 100), 100);
  assert.equal(scaleY(10, 0, 10, 100), 0);
  assert.equal(scaleY(5, 0, 10, 100), 50);
});

test('scaleY évite la division par zéro (série plate)', () => {
  assert.equal(scaleY(5, 5, 5, 100), 50);
});

test('linePath génère un path à partir des valeurs', () => {
  const d = linePath([0, 5, 10], { width: 100, height: 100, min: 0, max: 10 });
  assert.match(d, /^M0,100 L50,50 L100,0$/);
});
```

- [ ] **Step 2 : Lancer (échec attendu)**

Run: `node --test test/svg.test.mjs`
Expected : FAIL.

- [ ] **Step 3 : Écrire `scripts/lib/svg.mjs`**

```js
export function scaleY(v, min, max, height) {
  if (max === min) return height / 2;
  return Math.round((1 - (v - min) / (max - min)) * height);
}

// values: number[]; renvoie un attribut `d` (polyligne)
export function linePath(values, { width, height, min, max }) {
  const n = values.length;
  if (n === 0) return '';
  const step = n === 1 ? 0 : width / (n - 1);
  return values
    .map((v, i) => `${i === 0 ? 'M' : 'L'}${Math.round(i * step)},${scaleY(v, min, max, height)}`)
    .join(' ');
}
```

- [ ] **Step 4 : Relancer (succès attendu)**

Run: `node --test test/svg.test.mjs`
Expected : PASS.

- [ ] **Step 5 : Commit**

```bash
git add scripts/lib/svg.mjs test/svg.test.mjs
git commit -m "feat: SVG geometry helpers (TDD)"
```

### Task 2.3 : Composants Sparkline + MovementArrow + Trophy

**Files:**
- Create: `src/components/Sparkline.astro`, `MovementArrow.astro`, `Trophy.astro`

- [ ] **Step 1 : `Sparkline.astro`**

```astro
---
import { linePath } from '../../scripts/lib/svg.mjs';
const { points = [], color = 'var(--gold)', w = 64, h = 20 } = Astro.props;
const vals = points.map((p) => p.totalPoints);
const min = Math.min(...vals, 0);
const max = Math.max(...vals, 1);
const d = linePath(vals, { width: w, height: h, min, max });
---
{vals.length > 1 ? (
  <svg width={w} height={h} viewBox={`0 0 ${w} ${h}`} aria-hidden="true">
    <path d={d} fill="none" stroke={color} stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
  </svg>
) : <span class="dim" style="font-size:11px">—</span>}
```

- [ ] **Step 2 : `MovementArrow.astro`**

```astro
---
const { delta = 0 } = Astro.props; // rankDelta : >0 monte
const up = delta > 0, down = delta < 0;
const color = up ? 'var(--green)' : down ? 'var(--red)' : 'var(--text-dim)';
const sym = up ? '▲' : down ? '▼' : '–';
---
<span style={`color:${color};font-size:11px;font-weight:800`} title={`${delta} place(s)`}>
  {sym}{delta !== 0 ? Math.abs(delta) : ''}
</span>
```

- [ ] **Step 3 : `Trophy.astro` (trophée SVG or)**

```astro
---
const { size = 28 } = Astro.props;
---
<svg width={size} height={size} viewBox="0 0 24 24" fill="none" aria-hidden="true">
  <path d="M6 4h12v3a6 6 0 0 1-12 0V4z" fill="var(--gold)"/>
  <path d="M6 5H3v2a3 3 0 0 0 3 3M18 5h3v2a3 3 0 0 1-3 3" stroke="var(--gold)" stroke-width="1.5" fill="none"/>
  <rect x="9" y="13" width="6" height="3" fill="var(--gold)"/>
  <rect x="7" y="18" width="10" height="2.5" rx="1" fill="var(--gold)"/>
</svg>
```

- [ ] **Step 4 : Build + commit**

Run: `pnpm run build`
```bash
git add src/components/Sparkline.astro src/components/MovementArrow.astro src/components/Trophy.astro
git commit -m "feat: sparkline, movement arrow, trophy components"
```

### Task 2.4 : Podium + LeaderboardRow

**Files:**
- Create: `src/components/Podium.astro`, `src/components/LeaderboardRow.astro`

- [ ] **Step 1 : `LeaderboardRow.astro`**

```astro
---
import Sparkline from './Sparkline.astro';
import MovementArrow from './MovementArrow.astro';
import { accentFor } from '../../scripts/lib/members.mjs';
const { m, index, spark = [], me = false } = Astro.props;
---
<div class={`pill row${me ? ' me' : ''}`} data-key={m.id || m.name}
     style={`display:flex;align-items:center;gap:11px;padding:11px 16px;margin:6px 0;${me ? 'box-shadow:inset 0 0 0 1px var(--gold-soft);background:var(--gold-soft)' : ''}`}>
  <span style="width:20px;text-align:center;font-weight:800;color:var(--gold)">{m.rank}</span>
  <span style={`width:8px;height:8px;border-radius:50%;background:${accentFor(index)}`}></span>
  <span style="flex:1;font-weight:600">{m.name}{m.isAdmin && <span class="dim" style="font-size:10px"> · admin</span>}</span>
  <span class="dim" style="display:none;font-size:11px"></span>
  <Sparkline points={spark} />
  <MovementArrow delta={m.rankDelta} />
  <span style="font-weight:800;min-width:28px;text-align:right">{m.totalPoints}</span>
</div>
```

- [ ] **Step 2 : `Podium.astro` (top 3)**

```astro
---
import Trophy from './Trophy.astro';
const { top = [] } = Astro.props; // 3 premiers
const order = [1, 0, 2]; // 2e, 1er, 3e visuellement
const heights = { 0: 96, 1: 72, 2: 56 };
const medals = ['var(--gold)', '#c9cdd6', '#cd7f4d'];
---
<div style="display:flex;align-items:flex-end;justify-content:center;gap:10px;margin:8px 0 20px">
  {order.filter((i) => top[i]).map((i) => (
    <div style="flex:1;max-width:140px;text-align:center">
      {i === 0 && <div style="margin-bottom:4px"><Trophy size={30} /></div>}
      <div style="font-weight:700;font-size:13px">{top[i].name}</div>
      <div style="font-weight:900;color:var(--gold);font-size:20px">{top[i].totalPoints}</div>
      <div class="pill" style={`height:${heights[i]}px;border-top:3px solid ${medals[i]};border-radius:14px 14px 0 0;display:flex;align-items:flex-start;justify-content:center;padding-top:6px;font-weight:900;color:${medals[i]}`}>
        {top[i].rank}
      </div>
    </div>
  ))}
</div>
```

- [ ] **Step 3 : Build + commit**

Run: `pnpm run build`
```bash
git add src/components/Podium.astro src/components/LeaderboardRow.astro
git commit -m "feat: podium + leaderboard row components"
```

### Task 2.5 : Page Classement complète

**Files:**
- Modify: `src/pages/index.astro`

- [ ] **Step 1 : Écrire la page (podium, lignes, ligne experts, badge LIVE, MAJ, bouton « c'est moi »)**

```astro
---
import Base from '../layouts/Base.astro';
import Streaks from '../components/Streaks.astro';
import Podium from '../components/Podium.astro';
import LeaderboardRow from '../components/LeaderboardRow.astro';
import { getLatest, getTimeseries, keyOf } from '../data';

const latest = getLatest();
const ts = getTimeseries();
const top3 = latest.members.slice(0, 3);
const rest = latest.members;
const updated = new Date(latest.takenAt).toLocaleString('fr-CH', { dateStyle: 'medium', timeStyle: 'short' });
const expertsBest = Math.max(...(latest.experts.length ? latest.experts : [0]));
---
<Base title="Classement">
  <section style="position:relative">
    <div style="position:relative;height:120px">
      <Streaks height={120} />
      <div style="position:absolute;right:0;top:-6px;font:900 italic 96px/1 var(--font);color:transparent;-webkit-text-stroke:2px var(--gold-soft)">26</div>
      <div style="position:relative;z-index:1;padding-top:18px">
        <p class="kicker">★ Marvelous Digital</p>
        <h1 class="h1">Le <em>Classement</em></h1>
      </div>
    </div>
    <p class="dim" style="font-size:12px;margin:0 0 14px">
      Dernière mise à jour : {updated} · <a href="#methode" style="color:var(--gold)">comment ça marche</a>
    </p>

    <Podium top={top3} />

    {rest.map((m, i) => (
      <LeaderboardRow m={m} index={i} spark={ts[keyOf(m)] || []} />
    ))}

    <div class="pill" style="display:flex;align-items:center;gap:11px;padding:11px 16px;margin:14px 0 6px;border:1px dashed var(--line);background:transparent">
      <span style="width:20px;text-align:center">🤖</span>
      <span style="flex:1;font-weight:600" class="dim">Meilleur expert RTS</span>
      <span style="font-weight:800">{expertsBest}</span>
    </div>

    <button id="me-btn" class="pill" style="margin-top:16px;padding:8px 14px;border:1px solid var(--gold-soft);background:transparent;color:var(--gold);font-weight:700;cursor:pointer;border-radius:30px;font-size:13px">
      C'est moi → choisir mon nom
    </button>

    <p id="methode" class="dim" style="font-size:12px;margin-top:24px">
      Les points proviennent du jeu de pronostics RTS et sont rafraîchis automatiquement plusieurs fois par jour.
    </p>
  </section>

  <script>
    const KEY = 'marvelous-me';
    const btn = document.getElementById('me-btn');
    function highlight(name) {
      document.querySelectorAll('.row').forEach((r) => {
        if (r.dataset.key === name) {
          r.style.background = 'var(--gold-soft)';
          r.style.boxShadow = 'inset 0 0 0 1px var(--gold-soft)';
        }
      });
    }
    const saved = localStorage.getItem(KEY);
    if (saved) highlight(saved);
    btn?.addEventListener('click', () => {
      const names = [...document.querySelectorAll('.row')].map((r) => r.dataset.key);
      const choice = prompt('Ton nom dans le classement :\n' + names.join('\n'));
      if (choice && names.includes(choice)) { localStorage.setItem(KEY, choice); highlight(choice); }
    });
  </script>
</Base>
```

- [ ] **Step 2 : Build + vérification visuelle locale**

Run: `pnpm run dev` puis ouvrir l'URL locale ; vérifier podium, lignes, sparklines, badge experts.
Run: `pnpm run build`
Expected : succès.

- [ ] **Step 3 : Commit**

```bash
git add src/pages/index.astro
git commit -m "feat: page Classement (podium, lignes, experts, c'est moi)"
```

> **Jalon Phase 2 :** classement live et beau, déployé. ✅

---

# Phase 3 — Le Mag (stats & data-viz)

### Task 3.1 : Stats dérivées (TDD)

**Files:**
- Create: `test/stats.test.mjs`, `scripts/lib/stats.mjs`

- [ ] **Step 1 : Écrire `test/stats.test.mjs`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { biggestClimber, momentum, badges } from '../scripts/lib/stats.mjs';

const ts = {
  a: [ { totalPoints: 5, rank: 2, name: 'A' }, { totalPoints: 7, rank: 2, name: 'A' }, { totalPoints: 14, rank: 1, name: 'A' } ],
  b: [ { totalPoints: 6, rank: 1, name: 'B' }, { totalPoints: 9, rank: 1, name: 'B' }, { totalPoints: 10, rank: 2, name: 'B' } ],
};

test('biggestClimber = plus gros gain de points sur le dernier pas', () => {
  const c = biggestClimber(ts);
  assert.equal(c.name, 'A');
  assert.equal(c.gain, 7); // 14 - 7
});

test('momentum = somme des gains sur les 2 derniers pas', () => {
  assert.equal(momentum(ts.a), 9); // (7-5)+(14-7)
});

test('badges attribue La Remontada au meilleur grimpeur de rang', () => {
  const b = badges(ts);
  const remontada = b.find((x) => x.label === 'La Remontada');
  assert.equal(remontada.name, 'A'); // rang 2 -> 1
});
```

- [ ] **Step 2 : Lancer (échec attendu)**

Run: `node --test test/stats.test.mjs`
Expected : FAIL.

- [ ] **Step 3 : Écrire `scripts/lib/stats.mjs`**

```js
export function biggestClimber(ts) {
  let best = { name: null, gain: -Infinity };
  for (const series of Object.values(ts)) {
    if (series.length < 2) continue;
    const gain = series.at(-1).totalPoints - series.at(-2).totalPoints;
    if (gain > best.gain) best = { name: series.at(-1).name, gain };
  }
  return best;
}

export function momentum(series) {
  if (series.length < 2) return 0;
  const last3 = series.slice(-3);
  let sum = 0;
  for (let i = 1; i < last3.length; i++) sum += last3[i].totalPoints - last3[i - 1].totalPoints;
  return sum;
}

export function badges(ts) {
  const out = [];
  // La Remontada : plus grosse amélioration de rang (premier → dernier)
  let remontada = { name: null, gain: -Infinity };
  let regulier = { name: null, variance: Infinity };
  for (const series of Object.values(ts)) {
    if (series.length < 2) continue;
    const rankGain = series[0].rank - series.at(-1).rank;
    if (rankGain > remontada.gain) remontada = { name: series.at(-1).name, gain: rankGain };
    const gains = [];
    for (let i = 1; i < series.length; i++) gains.push(series[i].totalPoints - series[i - 1].totalPoints);
    const mean = gains.reduce((a, b) => a + b, 0) / gains.length;
    const variance = gains.reduce((a, b) => a + (b - mean) ** 2, 0) / gains.length;
    if (variance < regulier.variance) regulier = { name: series.at(-1).name, variance };
  }
  if (remontada.name) out.push({ label: 'La Remontada', name: remontada.name, hint: `${remontada.gain} place(s)` });
  if (regulier.name) out.push({ label: 'Le Régulier', name: regulier.name, hint: 'le plus constant' });
  return out;
}
```

- [ ] **Step 4 : Relancer (succès attendu)**

Run: `node --test test/stats.test.mjs`
Expected : PASS.

- [ ] **Step 5 : Commit**

```bash
git add scripts/lib/stats.mjs test/stats.test.mjs
git commit -m "feat: derived stats (climber, momentum, badges) (TDD)"
```

### Task 3.2 : RaceChart (évolution des points)

**Files:**
- Create: `src/components/RaceChart.astro`

- [ ] **Step 1 : Écrire `RaceChart.astro` (une polyligne par membre via `linePath`)**

```astro
---
import { linePath } from '../../scripts/lib/svg.mjs';
import { accentFor } from '../../scripts/lib/members.mjs';
const { ts, experts = [] } = Astro.props;
const W = 640, H = 260;
const series = Object.values(ts).filter((s) => s.length > 1);
const allPts = series.flatMap((s) => s.map((p) => p.totalPoints));
const min = Math.min(...allPts, 0);
const max = Math.max(...allPts, ...(experts.length ? experts : [1]));
const lines = series.map((s, i) => ({
  name: s.at(-1).name, color: accentFor(i),
  d: linePath(s.map((p) => p.totalPoints), { width: W, height: H, min, max }),
  end: { y: (1 - (s.at(-1).totalPoints - min) / (max - min || 1)) * H, v: s.at(-1).totalPoints },
}));
const expertY = experts.length ? (1 - (Math.max(...experts) - min) / (max - min || 1)) * H : null;
---
<div class="pill" style="padding:16px;overflow:hidden">
  <p class="kicker" style="margin-bottom:10px">La course en tête</p>
  <svg viewBox={`0 0 ${W + 80} ${H + 20}`} style="width:100%;height:auto">
    {expertY !== null && (
      <g>
        <line x1="0" y1={expertY} x2={W} y2={expertY} stroke="var(--text-dim)" stroke-dasharray="4 4" stroke-width="1"/>
        <text x={W + 4} y={expertY + 4} fill="var(--text-dim)" font-size="10">experts</text>
      </g>
    )}
    {lines.map((l) => (
      <g>
        <path d={l.d} fill="none" stroke={l.color} stroke-width="2.5" stroke-linejoin="round"/>
        <circle cx={W} cy={l.end.y} r="3" fill={l.color}/>
        <text x={W + 6} y={l.end.y + 4} fill={l.color} font-size="11" font-weight="700">{l.name} {l.end.v}</text>
      </g>
    ))}
  </svg>
  <p class="dim" style="font-size:11px;margin-top:8px">Points cumulés au fil des relevés.</p>
</div>
```

- [ ] **Step 2 : Build + commit**

Run: `pnpm run build`
```bash
git add src/components/RaceChart.astro
git commit -m "feat: RaceChart points evolution"
```

### Task 3.3 : OtherGroups + Badges + HeadToHead

**Files:**
- Create: `src/components/OtherGroups.astro`, `src/components/Badges.astro`, `src/components/HeadToHead.astro`

- [ ] **Step 1 : `OtherGroups.astro`**

```astro
---
const { groups = [] } = Astro.props;
---
<div class="pill" style="padding:16px">
  <p class="kicker" style="margin-bottom:10px">Marvelous vs les autres groupes</p>
  {groups.map((g) => (
    <div style="display:flex;gap:10px;padding:6px 0;border-bottom:1px solid var(--line)">
      <span style="color:var(--gold);font-weight:800;width:28px">#{g.rank}</span>
      <span style="flex:1">{g.name}</span>
      <span style="font-weight:800">{g.points}</span>
    </div>
  ))}
</div>
```

- [ ] **Step 2 : `Badges.astro`**

```astro
---
const { items = [] } = Astro.props;
---
<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px">
  {items.map((b) => (
    <div class="pill" style="padding:14px;border-radius:var(--radius-card)">
      <div class="kicker">{b.label}</div>
      <div style="font-weight:800;margin-top:4px">{b.name}</div>
      <div class="dim" style="font-size:11px">{b.hint}</div>
    </div>
  ))}
</div>
```

- [ ] **Step 3 : `HeadToHead.astro` (sélecteur client, deux membres)**

```astro
---
const { members = [] } = Astro.props; // [{name,totalPoints}]
const data = JSON.stringify(members);
---
<div class="pill" style="padding:16px">
  <p class="kicker" style="margin-bottom:10px">Duel</p>
  <div style="display:flex;gap:8px;flex-wrap:wrap">
    <select id="h2h-a" class="pill" style="padding:8px;background:var(--bg-soft);color:var(--text);border:1px solid var(--line)"></select>
    <span style="align-self:center;color:var(--gold);font-weight:800">VS</span>
    <select id="h2h-b" class="pill" style="padding:8px;background:var(--bg-soft);color:var(--text);border:1px solid var(--line)"></select>
  </div>
  <p id="h2h-out" style="margin-top:12px;font-weight:700"></p>
  <script set:html={`window.__H2H=${data}`}></script>
  <script>
    const M = window.__H2H || [];
    const a = document.getElementById('h2h-a'), b = document.getElementById('h2h-b'), out = document.getElementById('h2h-out');
    M.forEach((m, i) => { [a, b].forEach((sel, j) => { const o = document.createElement('option'); o.value = i; o.textContent = m.name; if ((j===0&&i===0)||(j===1&&i===1)) o.selected = true; sel.appendChild(o.cloneNode(true)); }); });
    function render() {
      const x = M[a.value], y = M[b.value]; if (!x || !y) return;
      const diff = x.totalPoints - y.totalPoints;
      out.textContent = diff === 0 ? `Égalité, ${x.totalPoints} partout !`
        : `${(diff>0?x:y).name} mène de ${Math.abs(diff)} pt(s).`;
    }
    a.addEventListener('change', render); b.addEventListener('change', render); render();
  </script>
</div>
```

- [ ] **Step 4 : Build + commit**

Run: `pnpm run build`
```bash
git add src/components/OtherGroups.astro src/components/Badges.astro src/components/HeadToHead.astro
git commit -m "feat: other groups, badges, head-to-head components"
```

### Task 3.4 : Page Le Mag

**Files:**
- Create: `src/pages/le-mag.astro`

- [ ] **Step 1 : Écrire la page**

```astro
---
import Base from '../layouts/Base.astro';
import RaceChart from '../components/RaceChart.astro';
import OtherGroups from '../components/OtherGroups.astro';
import Badges from '../components/Badges.astro';
import HeadToHead from '../components/HeadToHead.astro';
import { getLatest, getTimeseries } from '../data';
import { biggestClimber, badges } from '../../scripts/lib/stats.mjs';

const latest = getLatest();
const ts = getTimeseries();
const climber = biggestClimber(ts);
const allBadges = badges(ts);
if (climber.name && climber.gain > 0) allBadges.unshift({ label: 'Roi du dernier relevé', name: climber.name, hint: `+${climber.gain} pts` });
const h2hMembers = latest.members.map((m) => ({ name: m.name, totalPoints: m.totalPoints }));
---
<Base title="Le Mag">
  <p class="kicker">★ Stats & data</p>
  <h1 class="h1">Le <em>Mag</em></h1>
  <div style="display:flex;flex-direction:column;gap:16px;margin-top:16px">
    <RaceChart ts={ts} experts={latest.experts} />
    <Badges items={allBadges} />
    <HeadToHead members={h2hMembers} />
    <OtherGroups groups={latest.otherGroups} />
  </div>
</Base>
```

- [ ] **Step 2 : Build + commit**

Run: `pnpm run build`
```bash
git add src/pages/le-mag.astro
git commit -m "feat: page Le Mag"
```

> **Jalon Phase 3 :** stats & data-viz en ligne. ✅

---

# Phase 4 — Pronos de l'IA (L'Oracle)

### Task 4.1 : Barème RTS + scoring (TDD)

**Files:**
- Create: `test/scoring.test.mjs`, `scripts/lib/scoring.mjs`

> ✅ **Barème RTS confirmé** ([règlement officiel](https://pronostics.rts.ch/info/rules)) — cumulatif par match :
> - **Groupes** : bon résultat/nul = **5**, bon total de buts = **1**, bonne différence de buts (vainqueur correct) = **3** → max 9.
> - **Phases finales** : **10 / 2 / 6** → max 18.
> (Bonus tournoi 50 / questions bonus 20 = hors périmètre du joueur fantôme pour l'instant.)

- [ ] **Step 1 : Écrire `test/scoring.test.mjs`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { scorePrediction, ghostTotal } from '../scripts/lib/scoring.mjs';

test('score exact en groupes = 5+1+3 = 9', () => {
  assert.equal(scorePrediction({ home: 2, away: 1 }, { home: 2, away: 1 }), 9);
});
test('bon vainqueur seul = 5', () => {
  assert.equal(scorePrediction({ home: 2, away: 0 }, { home: 1, away: 0 }), 5);
});
test('bon vainqueur + bonne diff, mauvais total = 5+3 = 8', () => {
  assert.equal(scorePrediction({ home: 2, away: 0 }, { home: 3, away: 1 }), 8);
});
test('mauvais vainqueur mais bon total de buts = 1', () => {
  assert.equal(scorePrediction({ home: 0, away: 1 }, { home: 1, away: 0 }), 1);
});
test('nul exact = 9', () => {
  assert.equal(scorePrediction({ home: 1, away: 1 }, { home: 1, away: 1 }), 9);
});
test('barème phases finales : exact = 10+2+6 = 18', () => {
  assert.equal(scorePrediction({ home: 2, away: 1 }, { home: 2, away: 1 }, 'knockout'), 18);
});
test('ghostTotal somme les pronos avec résultat connu (stage pris en compte)', () => {
  const preds = [
    { result: { home: 2, away: 1 }, actual: { home: 2, away: 1 }, stage: 'group' },    // 9
    { result: { home: 1, away: 0 }, actual: { home: 0, away: 0 }, stage: 'group' },     // nul réel, pred victoire → total 1 vs 0 non, diff non, outcome non = 0
    { result: { home: 1, away: 1 }, actual: null, stage: 'group' },                      // ignoré
  ];
  assert.equal(ghostTotal(preds), 9);
});
```

- [ ] **Step 2 : Lancer (échec attendu)**

Run: `node --test test/scoring.test.mjs`
Expected : FAIL.

- [ ] **Step 3 : Écrire `scripts/lib/scoring.mjs`**

```js
// Barème RTS officiel (cumulatif par match). Source : pronostics.rts.ch/info/rules
export const SCORING = {
  group:    { outcome: 5, totalGoals: 1, goalDiff: 3 },
  knockout: { outcome: 10, totalGoals: 2, goalDiff: 6 },
};

const sign = (a, b) => Math.sign(a - b);

export function scorePrediction(pred, actual, stage = 'group') {
  if (!pred || !actual) return 0;
  const s = SCORING[stage] || SCORING.group;
  let pts = 0;
  const outcomeOk = sign(pred.home, pred.away) === sign(actual.home, actual.away);
  if (outcomeOk) pts += s.outcome;                                   // bon vainqueur / nul
  if (pred.home + pred.away === actual.home + actual.away) pts += s.totalGoals; // bon total de buts (indépendant)
  if (outcomeOk && pred.home - pred.away === actual.home - actual.away) pts += s.goalDiff; // bonne diff + vainqueur correct
  return pts;
}

export function ghostTotal(preds) {
  return preds.reduce((sum, p) => sum + (p.actual ? scorePrediction(p.result, p.actual, p.stage) : 0), 0);
}
```

- [ ] **Step 4 : Relancer (succès attendu)**

Run: `node --test test/scoring.test.mjs`
Expected : PASS.

- [ ] **Step 5 : Commit**

```bash
git add scripts/lib/scoring.mjs test/scoring.test.mjs
git commit -m "feat: prediction scoring + ghost total (TDD)"
```

### Task 4.2 : Content collection des pronos

**Files:**
- Create: `src/content/config.ts`
- Create: `src/content/pronos/groupe-j1.md` (gabarit + 1 exemple complet)

- [ ] **Step 1 : Écrire `src/content/config.ts`**

```ts
import { defineCollection, z } from 'astro:content';

const matchSchema = z.object({
  home: z.string(), away: z.string(),
  homeFlag: z.string().optional(), awayFlag: z.string().optional(),
  kickoff: z.string(),                       // ISO
  predHome: z.number(), predAway: z.number(),
  confidence: z.number().min(1).max(5),
  analysis: z.string(),
  actualHome: z.number().nullable().default(null),
  actualAway: z.number().nullable().default(null),
});

const pronos = defineCollection({
  type: 'content',
  schema: z.object({
    round: z.string(),       // "Journée 1", "8es de finale"...
    order: z.number(),       // tri d'affichage
    matches: z.array(matchSchema),
  }),
});

export const collections = { pronos };
```

- [ ] **Step 2 : Écrire `src/content/pronos/groupe-j1.md` (front-matter, 1 match d'exemple — le reste sera rempli en Task 4.5)**

```md
---
round: "Journée 1"
order: 1
matches:
  - home: "Suisse"
    away: "Mexique"
    homeFlag: "🇨🇭"
    awayFlag: "🇲🇽"
    kickoff: "2026-06-13T18:00:00Z"
    predHome: 1
    predAway: 1
    confidence: 3
    analysis: "Exemple de gabarit — à remplacer par l'analyse réelle (forme, historique, infos)."
    actualHome: null
    actualAway: null
---
Pronostics IA — Journée 1.
```

- [ ] **Step 3 : Build + commit**

Run: `pnpm run build`
Expected : la collection compile.
```bash
git add src/content/config.ts src/content/pronos/groupe-j1.md
git commit -m "feat: pronos content collection + gabarit J1"
```

### Task 4.3 : Page Pronos de l'IA + joueur fantôme

**Files:**
- Create: `src/components/MatchCard.astro`, `src/pages/pronos-ia.astro`

- [ ] **Step 1 : `MatchCard.astro`**

```astro
---
import { scorePrediction } from '../../scripts/lib/scoring.mjs';
const { m } = Astro.props;
const played = m.actualHome !== null && m.actualAway !== null;
const pts = played ? scorePrediction({ home: m.predHome, away: m.predAway }, { home: m.actualHome, away: m.actualAway }) : null;
const ok = pts !== null && pts > 0;
const ko = new Date(m.kickoff).toLocaleString('fr-CH', { day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit' });
---
<div class="pill" style="padding:14px 16px;margin:8px 0">
  <div style="display:flex;align-items:center;gap:8px">
    <span style="flex:1;text-align:right;font-weight:700">{m.homeFlag} {m.home}</span>
    <span class="pill" style="padding:4px 10px;font-weight:900;color:var(--gold);background:var(--bg-soft)">{m.predHome}–{m.predAway}</span>
    <span style="flex:1;font-weight:700">{m.away} {m.awayFlag}</span>
  </div>
  <div style="display:flex;align-items:center;gap:8px;margin-top:8px">
    <span class="dim" style="font-size:11px">{ko}</span>
    <span style="color:var(--gold);font-size:11px">{'●'.repeat(m.confidence)}<span class="dim">{'●'.repeat(5 - m.confidence)}</span></span>
    {played && <span style={`margin-left:auto;font-weight:800;color:${ok ? 'var(--green)' : 'var(--red)'}`}>{ok ? `✓ +${pts}` : '✗ 0'} · réel {m.actualHome}–{m.actualAway}</span>}
  </div>
  <p class="dim" style="font-size:12px;margin:8px 0 0">{m.analysis}</p>
</div>
```

- [ ] **Step 2 : `src/pages/pronos-ia.astro` (groupé par round + total fantôme)**

```astro
---
import Base from '../layouts/Base.astro';
import MatchCard from '../components/MatchCard.astro';
import { getCollection } from 'astro:content';
import { ghostTotal } from '../../scripts/lib/scoring.mjs';
import { getLatest } from '../data';

const entries = (await getCollection('pronos')).sort((a, b) => a.data.order - b.data.order);
const allMatches = entries.flatMap((e) => e.data.matches);
const ghost = ghostTotal(allMatches.map((m) => ({
  result: { home: m.predHome, away: m.predAway },
  actual: m.actualHome !== null ? { home: m.actualHome, away: m.actualAway } : null,
})));
const human = getLatest().members.map((m) => m.totalPoints);
const ghostRank = 1 + human.filter((p) => p > ghost).length;
---
<Base title="Pronos IA">
  <p class="kicker">🤖 L'Oracle — pronostics générés par l'IA</p>
  <h1 class="h1">Les Pronos de <em>l'IA</em></h1>
  <p class="dim" style="font-size:13px">Ce ne sont <strong>pas</strong> les pronos des membres : ce sont mes analyses (Claude). Si l'IA jouait, elle serait <strong style="color:var(--gold)">{ghostRank}e</strong> avec <strong>{ghost}</strong> pts.</p>

  {entries.map((e) => (
    <section style="margin-top:22px">
      <p class="kicker">{e.data.round}</p>
      {e.data.matches.map((m) => <MatchCard m={m} />)}
    </section>
  ))}
</Base>
```

- [ ] **Step 3 : Build + commit**

Run: `pnpm run build`
```bash
git add src/components/MatchCard.astro src/pages/pronos-ia.astro
git commit -m "feat: page Pronos de l'IA + joueur fantôme"
```

### Task 4.4 : Bracket prédictif (phases finales)

**Files:**
- Create: `src/components/Bracket.astro`
- Modify: `src/pages/pronos-ia.astro` (insérer le bracket)

- [ ] **Step 1 : `Bracket.astro` (arbre simple à partir d'une liste de tours)**

```astro
---
// rounds: [{ name, ties: [{a,b,winner}] }]
const { rounds = [] } = Astro.props;
---
<div style="display:flex;gap:18px;overflow-x:auto;padding:6px 0">
  {rounds.map((r) => (
    <div style="min-width:150px;display:flex;flex-direction:column;justify-content:space-around;gap:10px">
      <p class="kicker">{r.name}</p>
      {r.ties.map((t) => (
        <div class="pill" style="padding:8px 10px;font-size:12px">
          <div style={t.winner === t.a ? 'font-weight:800;color:var(--gold)' : 'color:var(--text-dim)'}>{t.a}</div>
          <div style={t.winner === t.b ? 'font-weight:800;color:var(--gold)' : 'color:var(--text-dim)'}>{t.b}</div>
        </div>
      ))}
    </div>
  ))}
</div>
```

- [ ] **Step 2 : Ajouter un bloc bracket optionnel dans `pronos-ia.astro`**

Juste avant `</Base>`, ajouter (les données viendront d'un fichier de contenu quand les phases finales arrivent ; tableau vide = section masquée) :
```astro
---
// en tête de frontmatter, après les imports existants :
import Bracket from '../components/Bracket.astro';
const bracket: any[] = []; // rempli en Task 4.5 (phases finales)
---
```
Et dans le corps, avant `</Base>` :
```astro
{bracket.length > 0 && (
  <section style="margin-top:26px">
    <p class="kicker">Bracket prédictif</p>
    <Bracket rounds={bracket} />
  </section>
)}
```

- [ ] **Step 3 : Build + commit**

Run: `pnpm run build`
```bash
git add src/components/Bracket.astro src/pages/pronos-ia.astro
git commit -m "feat: predictive bracket (masqué tant que vide)"
```

### Task 4.5 : Générer le contenu des pronos J1 (recherche réelle)

**Files:**
- Modify: `src/content/pronos/groupe-j1.md`

> Cette tâche est du **contenu**, pas du code. Elle requiert une recherche web au moment de l'exécution.

- [ ] **Step 1 : Récupérer le calendrier réel de la Journée 1**

Identifier les matchs réels de la J1 de la CDM 2026 (équipes, dates/heures de coup d'envoi UTC) via recherche web (source : calendrier officiel FIFA / RTS).

- [ ] **Step 2 : Pour chaque match J1, rechercher et rédiger**

Pour chaque match : forme récente des 2 équipes, historique des confrontations, infos (blessures/compositions). En déduire `predHome`/`predAway`, `confidence` (1–5), et une `analysis` de 1–2 phrases. Remplir le tableau `matches:` dans `groupe-j1.md` au format du schéma (Task 4.2).

- [ ] **Step 3 : Build + vérification**

Run: `pnpm run build`
Expected : la page Pronos affiche tous les matchs J1.

- [ ] **Step 4 : Commit**

```bash
git add src/content/pronos/groupe-j1.md
git commit -m "content: pronos IA Journée 1"
```

> Répéter le schéma de Task 4.5 pour J2, J3, puis chaque tour final (nouveaux fichiers `groupe-j2.md`, `groupe-j3.md`, `finales-8e.md`, …) au fur et à mesure du tournoi.

> **Jalon Phase 4 :** pronos IA + joueur fantôme en ligne. ✅

---

# Phase 5 — Page Matchs (calendrier)

### Task 5.1 : Calendrier à partir des pronos

**Files:**
- Create: `src/pages/matchs.astro`

- [ ] **Step 1 : Écrire la page (tous les matchs de la collection, triés par coup d'envoi)**

```astro
---
import Base from '../layouts/Base.astro';
import MatchCard from '../components/MatchCard.astro';
import { getCollection } from 'astro:content';

const matches = (await getCollection('pronos'))
  .flatMap((e) => e.data.matches)
  .sort((a, b) => a.kickoff.localeCompare(b.kickoff));
const now = new Date().toISOString();
const upcoming = matches.filter((m) => m.kickoff >= now);
const past = matches.filter((m) => m.kickoff < now);
---
<Base title="Matchs">
  <p class="kicker">★ Calendrier</p>
  <h1 class="h1">Les <em>Matchs</em></h1>
  {upcoming.length > 0 && <section style="margin-top:16px"><p class="kicker">À venir</p>{upcoming.map((m) => <MatchCard m={m} />)}</section>}
  {past.length > 0 && <section style="margin-top:20px"><p class="kicker">Joués</p>{past.map((m) => <MatchCard m={m} />)}</section>}
  <p class="dim" style="font-size:12px;margin-top:20px">Bonus à venir : ce que chaque membre a pronostiqué, une fois le match commencé.</p>
</Base>
```

- [ ] **Step 2 : Build + commit**

Run: `pnpm run build`
```bash
git add src/pages/matchs.astro
git commit -m "feat: page Matchs (calendrier)"
```

> **Note bonus (post-MVP, hors plan strict) :** l'affichage des pronos par membre nécessite de scraper les profils (`/users/<id>`) après coup d'envoi. À traiter quand l'extraction SSR est confirmée ; brancher dans une étape ultérieure sans bloquer le reste.

> **Jalon Phase 5 :** les 4 pages sont en ligne. ✅

---

# Phase 6 — Finitions & mise en ligne

### Task 6.1 : Image OG de partage

**Files:**
- Create: `public/og.png` (ou `public/og.svg`)
- Modify: `src/layouts/Base.astro`

- [ ] **Step 1 : Créer une image OG simple (1200×630) aux couleurs Tri-Streak Noir** (fond `#0a0e1a`, « Marvelous 26 » en or, rubans tricolores). Exporter en `public/og.png`.

- [ ] **Step 2 : Ajouter les meta OG dans `Base.astro` `<head>`**

```astro
<meta property="og:title" content={SITE.title} />
<meta property="og:description" content={SITE.tagline} />
<meta property="og:image" content={`${SITE.title ? '' : ''}${import.meta.env.SITE || ''}${import.meta.env.BASE_URL}og.png`} />
<meta name="twitter:card" content="summary_large_image" />
```

- [ ] **Step 3 : Build + commit**

Run: `pnpm run build`
```bash
git add public/og.png src/layouts/Base.astro
git commit -m "feat: OG share image"
```

### Task 6.2 : Confettis au changement de leader

**Files:**
- Modify: `src/pages/index.astro`

- [ ] **Step 1 : Ajouter un script léger (sans dépendance) qui compare le leader mémorisé en `localStorage` et lance une petite animation CSS de confettis si le nom du #1 a changé.**

```astro
<script>
  const LKEY = 'marvelous-leader';
  const leader = document.querySelector('.row')?.dataset.key;
  const prev = localStorage.getItem(LKEY);
  if (leader && prev && prev !== leader) {
    const c = document.createElement('div');
    c.textContent = '🎉';
    c.style.cssText = 'position:fixed;inset:0;display:flex;align-items:center;justify-content:center;font-size:80px;pointer-events:none;animation:pop 1.2s ease forwards;z-index:50';
    document.body.appendChild(c);
    setTimeout(() => c.remove(), 1300);
  }
  if (leader) localStorage.setItem(LKEY, leader);
</script>
<style>@keyframes pop{0%{transform:scale(.3);opacity:0}40%{transform:scale(1.2);opacity:1}100%{transform:scale(1);opacity:0}}</style>
```

- [ ] **Step 2 : Build + commit**

Run: `pnpm run build`
```bash
git add src/pages/index.astro
git commit -m "feat: confetti on leader change"
```

### Task 6.3 : QA finale + tests + mise en ligne

- [ ] **Step 1 : Lancer toute la suite de tests**

Run: `node --test`
Expected : tous les fichiers `test/*.test.mjs` PASS.

- [ ] **Step 2 : Build final**

Run: `pnpm run build`
Expected : succès, `dist/` complet.

- [ ] **Step 3 : (Manuel utilisateur) Pousser sur GitHub**

```bash
git remote add origin https://github.com/nicolasloi/CDM-prono.git
git branch -M main
git push -u origin main
```
Puis activer Pages (Settings → Pages → Source = GitHub Actions) et lancer une fois `Scrape RTS` manuellement.

- [ ] **Step 4 : Dogfood mobile**

Ouvrir l'URL Pages sur téléphone : vérifier les 4 pages, lisibilité, sparklines, RaceChart, lien RTS.

> **Jalon final :** site complet en ligne, auto-rafraîchi. 🏆

---

## Auto-revue (couverture spec)

- §2 Source de données → Tasks 1.2–1.4 (parser, scrape) ✅
- §3 Architecture/pipeline → Tasks 1.3–1.5 ✅
- §4 Fréquence refresh → Task 1.5 (crons) ✅
- §5 Page Classement → Phase 2 ✅ ; Le Mag → Phase 3 ✅ ; Pronos IA (nommée explicitement) → Phase 4 ✅ ; Matchs → Phase 5 ✅
- §5 lien RTS header+footer → Task 0.3 ✅
- §6 Pronos par journée + scoring ✓/✗ + ghost → Tasks 4.1–4.5 ✅
- §7 Identité Tri-Streak Noir → Tasks 0.3 (tokens, Streaks), 2.x, 6.1 ✅
- §8 Stack Astro + SVG fait main → toutes phases ✅
- §9 Déploiement Pages → Tasks 0.2, 6.3 ✅
- §10 Hors périmètre respecté (pas de login/DB) ✅
- §11 Risques : barème RTS (Task 4.1 ⚠️), extraction par membre (Phase 5 note bonus), mapping ids (parser), scraper défensif (Task 1.4) ✅
